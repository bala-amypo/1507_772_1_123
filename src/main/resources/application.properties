spring.application.name=demo
# Change this to 9238 to match your URL prefix
server.port=9238

# For https handling in proxy environments
server.forward-headers-strategy=framework 

# Database Configuration
# Ensure the database name matches your project (employee_skills_db) 
# or keep transport_pro if that is what was provided in your env
spring.datasource.url=jdbc:mysql://localhost:3306/transport_pro?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect